import React from 'react'
import { StyledContainer } from './styles'

export interface IDropPlaceholderProps {}

const DropPlaceholder: React.FC<IDropPlaceholderProps> = (props) => <StyledContainer {...props} />

export default DropPlaceholder
